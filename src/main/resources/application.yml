server:
  port: 8091
  tomcat:
    uri-encoding: utf-8
spring:
  # 配置可以直接访问的静态文件目录
  main:
    allow-bean-definition-overriding: true
  resources:
    static-locations: classpath:/static/,classpath:/templates/,classpath:/static/img/,classpath:/static/css/,classpath:/static/js/
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: XXX #47数据库新建schema的Url
    username: test1 #47mysql username
    password:  XXXX #47mysql password
    type: com.alibaba.druid.pool.DruidDataSource
beetl:
  suffix: html
